services:
  - type: web
    name: doctotext
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --timeout 600 --workers 4 --threads 2"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
